# Вопросы для экзамена 

**Дата проведения:** 15 января 2026 года  
**Время:** 8:50 – 12:15

**Структура билета:**

1. **Три теоретических вопроса** – дайте развернутый ответ, раскрывающий суть понятия или принципа.
2. **Одна практическая задача** – напишите код, решающий поставленную задачу, с пояснением подхода.
3. **Дополнительное задание** – выполняется студентами, имеющими академическую задолженность.

**15 минут на вопросы 25 на задачу**

**⭐️ - Бонус можно использовать, чтобы вытянуть новый билет.**

Одна ⭐️:
1. **Перевыбор билета на тесте/билет.**
2. **Дополнительные 15 минут на тест/билет.**
3. **Право на одну подсказку от преподавателя.** 

Десять ⭐️:
1. **Получить счастливый билет (В нем может быть меньше задач или они будут проще)**

---
# Теоретические вопросы

1.  Дайте определение массива как структуры данных.
2.  Объясните принцип работы связного списка.
3.  Дайте определение стека. Назовите его основной принцип работы.
4.  Дайте определение очереди. Назовите её основной принцип работы.
5.  Дайте определение графа как структуры данных.
6.  Дайте определение дерева как структуры данных.
7.  Дайте определение бинарного дерева.
8. Объясните, что такое хеш-таблица и для чего она используется.
9. Объясните, что такое хеш-функция.
10. Объясните, что такое коллизия в контексте хеш-таблиц.
11. Опишите алгоритм сортировки выбором.
12. Опишите алгоритм быстрой сортировки (quicksort).
13. Объясните, что такое асимптотическая сложность («О большое»). Приведите пример.
14. Объясните принцип «Разделяй и властвуй».
15. Объясните суть динамического программирования.
16. Объясните, что такое жадный алгоритм.
17. Объясните, что такое эвристический алгоритм.
18. Объясните принцип метода «ветвей и границ».
19. Опишите алгоритм поиска в ширину (BFS).
20. Опишите алгоритм поиска в глубину (DFS).
21.  Перечислите основные встроенные типы данных в Python и приведите примеры каждого.
22.  Объясните разницу между изменяемыми и неизменяемыми типами данных в Python.
23.  Дайте определение списка (list) и кортежа (tuple). В чём их ключевое различие?
24.  Объясните, как работает механизм ссылок в Python при операции присваивания списков.
25.  Дайте определение словаря (dict) и множества (set). Приведите примеры их использования.
26.  Объясните, как проверить тип переменной в Python.
27.  Объясните, что такое f-строки и как с их помощью форматировать строки.
28.  Объясните назначение ключевых слов `pass`, `break` и `continue`.
29.  Объясните, что такое срезы (slicing) в Python. Приведите пример.
30. Объясните, как работает оператор `in` для строк, списков и словарей.
31.  Объясните, как объявить функцию в Python. Дайте определение параметрам и аргументам функции.
32.  Объясните разницу между локальными и глобальными переменными. Как использовать ключевое слово `global`?
33.  Объясните, как импортировать модули в Python.
34.  Объясните, как создать собственный модуль в Python.
35.  Объясните, как передать в функцию неограниченное количество аргументов с помощью `*args` и `**kwargs`.
36.  Перечислите основные типы исключений в Python и приведите примеры.
37.  Объясните, как обрабатывать исключения с помощью конструкции `try-except-else-finally`.
38.  Объясните, как выбросить собственное исключение с помощью оператора `raise`.
39.  Объясните, как открыть файл на чтение или запись в Python. Зачем использовать менеджер контекста `with`?
40.  Объясните, как читать файл построчно в Python.
41.  Объясните, как записать список строк в текстовый файл.
42.  Объясните, что такое CSV-файл. Как с ним работать в Python?
43.  Объясните, как отправить HTTP-запрос с помощью библиотеки `requests` в Python.
44.  Объясните, что такое REST API и основные принципы работы с ним.
45.  Объясните, как работает простое веб-приложение на примере сервиса погоды (используя Flask или FastAPI).
46.  Перечислите основные компоненты веб-страницы и их назначение (HTML, CSS, JavaScript).
47.  Объясните, что такое клиент-серверная архитектура. Приведите примеры.
48.  Объясните разницу между статическим и динамическим веб-сайтом.
49.  Объясните, что такое HTTP и HTTPS. Зачем нужен протокол SSL/TLS?
50.  Объясните, что такое API. Приведите пример использования REST API.
51.  Объясните, как выполняется сложение и вычитание в двоичной системе счисления.
52.  Объясните, что такое прямой, обратный и дополнительный коды для представления целых чисел. Зачем они нужны?
53.  Объясните, почему при вычислениях с вещественными (дробными) числами возникают ошибки округления.
54.  Объясните, как в компьютере хранятся символы. Дайте определение кодировкам ASCII и Unicode.
55.  Объясните разницу между кодировками UTF-8, UTF-16 и UTF-32.
56.  Объясните, как представляются изображения в цифровом виде. В чём разница между растровой и векторной графикой?
57.  Объясните, что такое условный оператор. Приведите его синтаксис в Python.
58.  Объясните, как работают циклы `for` и `while`. Когда следует использовать каждый из них?
59.  Объясните, что такое модуль (библиотека) в Python. Как его подключить в программе?
60.  Объясните основные операции работы с файлами в Python (чтение, запись).
61.  Объясните, что такое исключение (exception) в Python. Как его обработать?
62.  Объясните, как организовать ввод данных от пользователя и вывод результатов в консольной программе на Python.
63.  Расшифруйте аббревиатуру MVC. Объясните роль и взаимодействие компонентов Model, View и Controller.
64.  Приведите пример реального приложения, построенного по паттерну MVC.
65.  Перечислите основные типы данных, используемые в анализе данных (например, числовые, категориальные, временные).
66.  Объясните, что такое структурированные, полуструктурированные и неструктурированные данные.
67.  Перечислите основные форматы для хранения табличных данных (например, CSV, Excel, Parquet) и их особенности.
68.  Объясните, что такое реляционная база данных. Приведите пример системы управления такими базами (СУБД).
69.  Объясните, как устроена таблица в реляционной базе данных. Дайте определение первичному и внешнему ключу.
70.  Объясните разницу между интерпретатором и компилятором.
71.  Объясните, как код на Python преобразуется в машинные инструкции (основные этапы).
72.  Объясните, что такое виртуальная машина в контексте выполнения программ.
73.  Перечислите основные встроенные типы данных в Python. Как проверить тип переменной?
74.  Перечислите основные группы операторов в Python (арифметические, логические, сравнения, присваивания, побитовые).
75.  Объясните, как работает условное выражение (`if-elif-else`) в Python.
76.  Объясните разницу между циклами `for` и `while`. В каких ситуациях предпочтительнее каждый из них?

# Задачи 

## Шифр Цезаря  
Напишите функцию, которая шифрует строку с использованием шифра Цезаря. Формула шифрования для каждого символа: `(x + n) mod 26`, где `x` — номер буквы в алфавите (0-25), `n` — сдвиг. Функция должна обрабатывать только буквы латинского алфавита (регистр сохраняется).  

Пример вывода:  
Вход: `"abc"`, сдвиг 3 → `"def"`

## ROT13  
Напишите функцию, которая применяет шифр ROT13 к строке (частный случай шифра Цезаря со сдвигом 13). Применение ROT13 к уже зашифрованному тексту возвращает исходный текст.  

Пример вывода:  
`"hello"` → `"uryyb"`

## Бинарное дерево поиска  
Реализуйте класс бинарного дерева поиска с методами добавления элемента, поиска элемента и обхода в порядке возрастания (in-order traversal). Продемонстрируйте работу на примере.

## Задача о рюкзаке
Даны предметы с весом и стоимостью. Напишите функцию, которая определяет максимальную суммарную стоимость предметов, которые можно уложить в рюкзак с ограничением по весу.  

Пример ввода:  
`items = [(2, 10), (3, 15), (5, 30)]`, `weight_limit = 5`  
Пример вывода:  
Максимальная стоимость = 25

## Грабитель
Даны предметы со стоимостью, временем укладки и весом. Напишите функцию, которая определяет максимальную стоимость предметов, которые можно уложить в рюкзак с ограничениями по весу и общему времени укладки.  

Пример ввода:  
`items = [(10, 5, 2), (15, 4, 3), (30, 7, 5)]`, `time_limit = 10`, `weight_limit = 10`  
Пример вывода:  
Максимальная стоимость = 45

## Числа Хэмминга  
Число Хэмминга — число вида $2^i * 3^j * 5^k$. Напишите функцию, которая возвращает n-ное наименьшее число Хэмминга.  

Пример вывода:  
`n = 5` → результат = 5

## Сумма делителей кратна сумме простых множителей
Напишите функцию, которая находит все числа в заданном диапазоне $[nMin, nMax]$, у которых сумма всех делителей делится на сумму простых множителей.  

Пример вывода:  
`nMin=12, nMax=119` → `[12, 63, 119]`

## НОД 

Реализовать функцию которая принимает два целых числа и возвращает их наибольший общий делитель (НОД)

## НОК

Реализовать функцию которая принимает два целых числа и возвращает их наименьшее общее кратное (НОК)
## Битовый счётчик НОД  
Напишите функцию, которая принимает два целых числа и возвращает количество единиц в двоичном представлении их наибольшего общего делителя (НОД).  

Пример вывода:  
`300, 45` → НОД = 15, двоичное представление 1111 → результат = 4

## Стек  
Реализуйте класс `Stack` (стек) с методами `push`, `pop`, `peek` и `is_empty`. Продемонстрируйте его работу.

## Очередь
Реализуйте класс `Queue` (очередь) с методами `enqueue`, `dequeue`, `peek` и `is_empty`. Продемонстрируйте его работу.

## Связный список
Реализуйте класс `LinkedList` (односвязный список) с методами добавления, удаления, поиска и вывода всех элементов.

## Двусвязный список  
Реализуйте класс `DoublyLinkedList` (двусвязный список) с методами добавления, удаления и обхода в обоих направлениях.

## Приоритетная очередь
Реализуйте приоритетную очередь, где элемент с наивысшим приоритетом извлекается первым. Продемонстрируйте её работу.

## Быстрая сортировка колоды карт  
Реализуйте алгоритм быстрой сортировки для сортировки колоды карт (представленной списком чисел). Используйте рекурсию и опорный элемент.  

Пример вывода:  
`[7, 2, 9, 5]` → `[2, 5, 7, 9]`

## Бинарный поиск  

Напишите функцию бинарного поиска в отсортированном списке. Возвращает индекс элемента или -1, если элемент не найден.  

Пример вывода:  
Список `[1, 3, 5, 7, 9]`, искомый элемент 5 → результат = 2

## Анаграммы  
Напишите функцию, которая проверяет, являются ли две строки анаграммами (содержат одни и те же буквы в одинаковом количестве).  

Пример вывода:  
`"listen"`, `"silent"` → `True`

## Палиндром  
Напишите функцию, которая проверяет, является ли строка палиндромом (читается одинаково слева направо и справа налево). Игнорируйте регистр и небуквенные символы.  

Пример вывода:  
`"A man, a plan, a canal: Panama"` → `True`

## Причудливые квадраты  
Напишите функцию, которая находит все числа с чётным количеством цифр, у которых квадрат суммы левой и правой половин равен самому числу (например, 3025 → (30+25)^2 = 3025).  

Пример вывода:  
Для 4 цифр: `[0001, 3025, 9801]` (или в другом формате)

## Делимость подстрок панцифрового числа
Найдите сумму всех панцифровых чисел (0-9, каждая цифра один раз), у которых подстроки $d2d3d4$, $d3d4d5$, … $d8d9d10$ делятся на простые числа 2, 3, 5, 7, 11, 13, 17 соответственно.  

## Тетрация  
Напишите рекурсивную функцию для вычисления тетрации $^n x$ (x возводится в степень x, n раз). Для небольших x и n.  

Пример вывода:  
`x = 2, n = 4` → результат = 65536

## Возвести каждую цифру в квадрат и склеить  
Напишите функцию, которая принимает целое число, возводит каждую его цифру в квадрат и возвращает результат как одно число (склеивая квадраты).  

Пример вывода:  
`9119` → `811181`

## Сумма кратных 3 или 5  
Напишите функцию, которая возвращает сумму всех натуральных чисел меньше заданного, кратных 3 или 5.  

Пример вывода:  
`number = 10` → сумма = 23

## Сумма чётных чисел Фибоначчи  

Напишите функцию, которая находит сумму всех чётных чисел в последовательности Фибоначчи, не превышающих заданный предел.  

Пример вывода:  
Предел = 100 → сумма = 44

## Разобрать число из слов  

Напишите функцию, которая преобразует строковое представление числа (на английском, до одного миллиона) в целое число. Игнорируйте слово "and".  

Пример вывода:  
`"seven hundred eighty-three thousand nine hundred nineteen"` → 783919

## Две суммы

Напишите функцию, которая принимает массив чисел и целевое число. Верните индексы двух чисел, сумма которых равна цели. Нельзя использовать один элемент дважды.  

Пример вывода:  
`nums = [2, 7, 11, 15]`, `target = 9` → `[0, 1]`

## Две суммы
Напишите функцию для отсортированного массива: найдите индексы двух чисел, сумма которых равна цели. Верните индексы в 1-индексации.  

Пример вывода:  
`nums = [2, 7, 11, 15]`, `target = 9` → `[1, 2]`

## Две суммы меньше K

Напишите функцию, которая находит максимальную сумму двух чисел в массиве, не превышающую K.  

Пример вывода:  
`nums = [34, 23, 1, 24, 75, 33]`, `K = 60` → максимальная сумма = 58

## На 3 суммы меньше

Напишите функцию, которая считает количество троек в массиве, сумма которых меньше заданного значения target.  

Пример вывода:  
`nums = [-2, 0, 1, 3]`, `target = 2` → количество троек = 2

## Подсчитайте особые четверни

Напишите функцию, которая считает количество четвёрок индексов ($a < b < c < d$), таких что `nums[a] + nums[b] + nums[c] = nums[d]`.  

Пример вывода:  
`nums = [1, 1, 1, 3, 5]` → количество = 1

## Выравнивание уровня воды в бочках 

Даны бочки с водой. Можно переливать воду из одной бочки в другую с потерей (коэффициент loss). Найдите максимальное одинаковое количество воды, которого можно достичь во всех бочках.  

Пример вывода:  
`buckets = [1, 2, 3, 4]`, `loss = 0.5` → результат ≈ 2.5

## accum  

Напишите функцию `accum`, которая преобразует строку: каждая буква повторяется столько раз, каков её порядковый номер в строке (первая — 1 раз, вторая — 2 раза и т.д.), при этом первое повторение выводится в верхнем регистре, а остальные — в нижнем. Элементы разделяются дефисами.  

Пример вывода:  
`accum("abcd")` → `"A-Bb-Ccc-Dddd"`

**Пирамида из банок**  
Напишите функцию `beeramid`, которая вычисляет количество полных уровней пирамиды из банок, которые можно купить, исходя из бонуса и цены банки. Каждый уровень содержит уровень² банок.  

Пример вывода:  
Бонус = 1500, цена банки = 2 → результат = 12

**Сокращение дроби**  
Напишите функцию, которая принимает дробь в виде`[числитель, знаменатель]` и возвращает её в несократимом виде.  

Пример вывода:  
`[45, 120]` -> `[3, 8]`

## Сортировка пузырьком
Реализуйте алгоритм сортировки пузырьком.

## Сортировка вставками
Реализуйте алгоритм сортировки вставками.